import router from '@/router'
import { Router } from 'vue-router'

/**
 * Initializes and returns the Vue Router instance.
 * @returns {Router} The initialized router.
 */
export const initRouter = (): Router => {
  return router
}
